import listSMS from '../../../utils/list-sms';
import { module, test } from 'qunit';

module('Unit | Utility | list sms');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = listSMS();
  assert.ok(result);
});
